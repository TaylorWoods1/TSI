[build-system]
requires = ["maturin>=1.4,<1.5"]
build-backend = "maturin"

[project]
name = "spyder"
version = "0.1.0"
description = "Parametric spider-cam inverse kinematics"
requires-python = ">=3.9"
license = { text = "MIT" }

[tool.maturin]
manifest-path = "Cargo.toml"
module-name = "spyder"
